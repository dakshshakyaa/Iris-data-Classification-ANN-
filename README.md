# Iris-data-Classification-ANN-
This project uses a Keras ANN to classify the Iris dataset into Setosa, Versicolor, and Virginica. Features are standardized, targets one-hot encoded. The model has multiple dense layers with ReLU and a softmax output, trained for 120 epochs, achieving accurate species classification.
